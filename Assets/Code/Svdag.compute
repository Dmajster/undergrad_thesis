#pragma kernel reduction

int packed_input_count;
StructuredBuffer<int> packed_input;

int packed_output_count;
RWStructuredBuffer<int> packed_output;

[numthreads(1, 1, 1)]
void reduction(const uint packed_output_index : SV_DispatchThreadID)
{
	for(uint bit = 0; bit < 32; bit++ )
	{
		const uint bit_index = packed_output_index * 32 + bit;
	} 
}